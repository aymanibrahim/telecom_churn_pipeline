# Telecom Customer Churn — pipeline runtime dependencies.
# Separate from dashboards/requirements-dashboards.txt (which is for Streamlit).
# Apache Airflow itself is installed in the Docker image, not here.

# --- Data ---
# numpy must be 2.x because shap 0.51.0 requires numpy>=2
pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0

# --- Distributed / large-CSV processing ---
dask[dataframe]==2024.12.1

# --- Geospatial ---
geopandas==1.0.1
shapely==2.0.6
pyproj==3.7.0
fiona==1.10.1

# --- ML ---
scikit-learn==1.6.1
lightgbm==4.6.0
xgboost==3.0.0
category_encoders==2.9.0
imbalanced-learn==0.14.1
joblib==1.5.3

# --- Explainability ---
shap==0.51.0

# --- Plotting (used by EDA + SHAP) ---
matplotlib==3.10.0
seaborn==0.13.2

# --- I/O / config ---
requests==2.32.3
PyYAML==6.0.2
python-dotenv==1.0.1

# --- Optional: experiment tracking (no-op unless MLFLOW_TRACKING_URI is set) ---
mlflow==2.18.0

# Streamlit
streamlit

# Flask REST API
flask==3.1.0
gunicorn==23.0.0

# dill is required to unpickle joblib payloads produced by training (LightGBM
# CalibratedClassifierCV uses dill via joblib for deep-pickling nested estimators).
dill==0.3.9
requests==2.32.3
