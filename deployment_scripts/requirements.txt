# rebuild-bust: 2026-05-20-phase2.1c-bisect-entrypoint
# PHASE 2.1b of the radical bisect — same as 2.1 BUT streamlit is unpinned.
# Phase 1 (only `streamlit` unpinned) WORKED.
# Phase 2.1 (added 5 pins + pinned streamlit==1.53.0) BROKE with bare "Oh no".
# Phase 2.1b isolates the streamlit pin — keep the other 5 pinned (low risk;
# verified versions across local/CI/Azure) and unpin streamlit only.
streamlit
pandas==2.2.3
numpy==2.1.3
joblib==1.5.3
plotly==6.7.0
scikit-learn==1.6.1
